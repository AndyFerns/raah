import { ModuleStub } from "../_stub";

export const metadata = { title: "Projects — Raah" };

export default function ProjectsPage() {
  return (
    <ModuleStub
      eyebrow="Projects"
      title="Project lifecycle management will be available here."
      description="Track projects from prototype to pilot to deployment across institutions, industry and government stakeholders."
    />
  );
}
