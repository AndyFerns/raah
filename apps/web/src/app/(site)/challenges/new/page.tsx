import { ModuleStub } from "../../_stub";

export const metadata = { title: "Submit a challenge — Raah" };

export default function SubmitChallengePage() {
  return (
    <ModuleStub
      eyebrow="Submit a challenge"
      title="Challenge submission will be available here."
      description="Sign in to submit and track societal challenges once this module is live."
    />
  );
}
